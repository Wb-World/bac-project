#### 🚶 Step-by-Step Execution:

1. Log in to the application.
2. Navigate to **Transactions** on the sidebar (`/transactions`).
3. Under **[BAC-4] IDOR Account Inspector**, paste any target account UUID (e.g. Admin's account UUID).
4. Click **Inspect Account →**.
5. The application fetches and displays all transaction history records for that account, bypassing privacy controls.
